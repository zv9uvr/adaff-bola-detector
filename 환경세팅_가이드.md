# BOLA AI 서버 실행 환경 세팅 가이드

## 1. Python 설치
- Python 3.11 설치 (반드시 3.11 버전)
- https://www.python.org/downloads/release/python-3119/
- 설치 시 "Add Python to PATH" 체크 필수

---

## 2. 패키지 설치 (터미널에 아래 명령어 복붙)

```
py -3.11 -m pip install fastapi uvicorn tensorflow keras scikit-learn numpy axios
```

하나씩 설치하고 싶으면:
```
py -3.11 -m pip install fastapi
py -3.11 -m pip install uvicorn
py -3.11 -m pip install tensorflow
py -3.11 -m pip install keras
py -3.11 -m pip install scikit-learn
py -3.11 -m pip install numpy
py -3.11 -m pip install requests
```

---

## 3. 필요한 파일 목록

```
📁 parsing/
├── ai_server_v8.py                ← AI 서버 메인
├── adaff_transformer_v8.keras     ← 학습된 모델
├── adaff_artifacts_v8.pkl         ← 전처리 스케일러
├── test_bola_model.py             ← 테스트 스크립트 (선택)
├── find_best_threshold.py         ← 임계값 탐색 스크립트 (선택)
├── X_test.npy                     ← 테스트 데이터 (선택)
└── y_test.npy                     ← 테스트 라벨 (선택)
```

---

## 4. 서버 실행

```
py -3.11 -m uvicorn ai_server_v8:app --port 8000
```

```
✅ 모델 및 스케일러 로드 완료. 서버 시작!
INFO: Uvicorn running on http://127.0.0.1:8000
```

---

## 5. 동작 확인

브라우저에서 http://127.0.0.1:8000/docs 접속
→ Swagger UI가 뜨면 정상

---

## 6. NestJS 인터셉터 연동 시 추가 설치
```
node 설치: https://nodejs.org (LTS 버전)
npm install @nestjs/common @nestjs/core axios
```

---

## 버전 정보 (테스트 완료 기준)
| 패키지 | 버전 |
|--------|------|
| Python | 3.11 |
| tensorflow / keras | 최신 버전 |
| scikit-learn | 1.6.x 권장 (1.8도 동작함) |
| fastapi | 최신 버전 |
| uvicorn | 최신 버전 |
| numpy | 최신 버전 |
